# Specify the directory where you want to clone the repository
destination_path  = "/monitor/backup1"
clone_command = "git clone https://user:token@github.com/user/github_test1.git"

clone_with_path = clone_command  +" "+ destination_path
os.system(clone_with_path)
